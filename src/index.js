import React from 'react';
import ReactDOM from 'react-dom/client';
import HSKApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <HSKApp />
  </React.StrictMode>
);